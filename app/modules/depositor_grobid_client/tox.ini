[tox]
envlist = c1

skip_missing_interpreters = true

[tool:pytest]
minversion = 3.0
testpaths = tests

[coverage:run]
source =
    depositor_grobid_client
    tests

[coverage:paths]
source =
    depositor_grobid_client
    tests
    .tox/*/lib/python*/site-packages/depositor_grobid_client
    .tox/*/lib/python*/site-packages/depositor_grobid_client/tests


[isort]
profile=black

[tool:isort]
line_length = 119

[testenv:c1]
passenv = LANG
deps =
    pytest>=3
    pytest-cov
	-rrequirements.txt
    setuptools
commands =
   # pytest --cov=weko_workflow tests -v --cov-branch --cov-report=term --basetemp="{envtmpdir}" {posargs}
   pytest --cov=depositor_grobid_client tests -v -s -vv --cov-branch --cov-report=term --cov-report=xml --cov-report=html --cov-config=tox.ini --basetemp="{envtmpdir}" {posargs}
